{
  "name": "maple-trackers-android",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:profit-expense": "node scripts/test-profit-expense-ui.cjs",
    "test:enhancements": "node scripts/test-enhancement-ledger.mjs && node scripts/test-enhancement-costs.mjs && node scripts/test-enhancement-ui.cjs",
    "test:ui": "node scripts/test-workspace-ui.cjs",
    "test:characters": "node scripts/test-character-world-transfer.cjs",
    "test:data-safety": "node scripts/test-hunt-data-safety.cjs && node scripts/test-cloud-sync-history.mjs && node scripts/test-cloud-sync-races.cjs",
    "prepare:android-web": "node scripts/prepare-android-web.mjs",
    "sync:android": "npm run prepare:android-web && npx cap sync android",
    "open:android": "npx cap open android",
    "run:android": "npm run sync:android && npx cap run android",
    "build:android:debug": "npm run sync:android && cd android && gradlew.bat assembleDebug",
    "build:android:bundle": "npm run sync:android && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "playwright": "^1.62.1"
  },
  "engines": {
    "node": ">=20"
  }
}

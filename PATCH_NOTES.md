# 패치노트 관리

사이트 변경 공지는 `assets/patch-notes-data.js`의 `MaplePatchNotesData.releases`로 관리합니다. 배열의 첫 공지를 최신 공지로 자동 표시합니다. 도움말의 **패치노트** 버튼에서는 숨긴 공지도 다시 열 수 있으며, 공지가 여러 개이면 날짜를 선택해 과거 공지를 읽을 수 있습니다.

## 새 변경을 배포할 때

1. 사용자에게 영향을 주는 변경을 한국어로 정리하고 `releases` 앞에 새 공지를 추가합니다.
2. 새 공지에는 고유한 `id`를 부여합니다. 예: `2026-09-19.1`. 같은 날의 별도 공지는 `.2`처럼 구분합니다.
3. `date`, `title`, `sections`의 `tag`·`title`·`items`, `footer`를 작성합니다. 내용은 HTML 없는 일반 텍스트입니다.
4. 이전 공지는 남겨 둡니다. 오탈자만 수정한다면 기존 `id`를 유지하고, 새로 안내할 변경이라면 새 `id`를 사용합니다.
5. `index.html`의 `patch-notes-data.js?v=...`를 새 값으로 바꿔 새 공지를 받도록 합니다. 동작·스타일 파일도 바꿨다면 각 파일의 버전도 갱신합니다.
6. `npm run test:patch-notes`와 변경 기능에 맞는 회귀 검증을 실행합니다. 날짜, 종료 시각, 실제 배포된 기능과 공지의 일치를 확인한 뒤 함께 배포합니다.

## 표시 및 저장 동작

- 첫 방문 및 숨기지 않은 공지는 새로 접속할 때 자동 표시합니다.
- ‘이 패치노트 다시 보지 않기’를 선택하고 **확인**을 눌렀을 때 해당 공지의 설정만 저장합니다.
- 닫기·Escape·배경 클릭은 창만 닫으며 체크박스 변경을 저장하지 않습니다.
- 숨긴 공지를 수동으로 열어 체크를 해제하고 확인하면 다시 자동 표시합니다.
- 새 공지는 다른 `id`이므로 이전 공지를 숨긴 이용자에게도 표시됩니다.
- 넥슨 로그인 복귀 중에는 자동 표시를 생략하고 수동 조회를 제공합니다.
- 저장소를 사용할 수 없으면 한국어 안내를 표시합니다. 닫기 버튼으로 사이트 이용을 계속할 수 있습니다.

설정은 해당 브라우저의 `maple_ui_patch_notes_hidden_v1` 키에 공지 ID 배열로 저장합니다. 장부 저장 함수, 클라우드 동기화, 복구 기록, 인증 세션에는 연결하지 않습니다. 장부 복구가 검색하는 `mapleTracker` 접두어도 사용하지 않습니다.

## 검증

전용 테스트는 자동 표시, 공지별 숨김·해제, 새 공지, 과거 공지 선택, 키보드와 모바일 메뉴, 내부 스크롤, 설정 저장 실패, OAuth 복귀, 장부·복구·인증 상태 보존을 확인합니다. 기존 화면 테스트는 `scripts/helpers/patch-notes.cjs`로 공지를 이미 숨긴 이용자 상태를 구성합니다. 운영 코드에는 테스트 우회 설정을 두지 않습니다.
